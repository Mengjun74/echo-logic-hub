# ═══════════════════════════════════════════════════════════════
# Echo-Logic Hub — Python Dependencies
# ═══════════════════════════════════════════════════════════════
# Install: pip install -r requirements.txt
#
# For mock-mode testing (no GPU), only streamlit, python-dotenv,
# google-generativeai, and numpy are strictly required.
# ═══════════════════════════════════════════════════════════════

# --- Core UI ---
streamlit>=1.31.0

# --- LLM ---
google-generativeai>=0.5.0

# --- Configuration ---
python-dotenv>=1.0.0

# --- Audio ---
PyAudio>=0.2.14
numpy>=1.24.0
soundfile>=0.12.0

# --- faster-whisper & pyannote (requires CUDA-capable GPU) ---
faster-whisper>=1.0.0
pyannote.audio>=3.1.0
webrtcvad-wheels>=2.0.10.post2
torchaudio>=2.1.0
